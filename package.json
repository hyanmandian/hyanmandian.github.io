{
  "dependencies": {
    "gatsby": "^1.9.247",
    "gatsby-link": "^1.6.40",
    "gatsby-plugin-react-helmet": "^2.0.10",
    "react-helmet": "^5.2.0",
    "sanitize.css": "^5.0.0",
    "styled-components": "^3.2.6"
  },
  "scripts": {
    "build": "gatsby build",
    "start": "gatsby develop",
    "deploy": "gatsby build --prefix-paths && gh-pages -d dist"
  },
  "devDependencies": {
    "gh-pages": "^1.1.0"
  }
}
