{
  "dependencies": {},
  "scripts": {
    "clean": "rimraf dist",
    "build:eleventy": "eleventy",
    "build:parcel": "parcel build src/assets/**/*.*",
    "prebuild": "npm run clean",
    "build": "run-p build:*",
    "start:eleventy": "eleventy --serve",
    "start:parcel": "parcel watch src/assets/**/*.*",
    "prestart": "npm run clean",
    "start": "run-p start:*"
  },
  "devDependencies": {
    "@11ty/eleventy": "^0.10.0",
    "npm-run-all": "^4.1.5",
    "parcel-bundler": "^1.12.4",
    "rimraf": "^3.0.2",
    "sanitize.css": "^11.0.0"
  }
}
